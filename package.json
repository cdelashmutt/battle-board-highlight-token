{
  "name": "battle-board-highlight-token",
  "version": "0.1.0",
  "description": "Highlights the token whose turn is active in Battle Board's initiative tracker.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@owlbear-rodeo/sdk": "^3.1.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
